# 06 — CI: test + publish workflows

Status: todo

Copy `.github/workflows/test.yml` and `publish.yml` from
`~/work/simonw/datasette-alerts-ntfy` and adapt.

## Adaptations

- Test matrix: 3.10–3.13 is enough.
- **Datasette source**: until a datasette alpha ships phase 1, CI must install datasette
  from the git branch:
  `pip install "datasette @ git+https://github.com/simonw/datasette@asg017/otel-phase1-4-otlp-demo"`
  (or the stack tip at the time). Leave a loud `# TODO: unpin when 1.0aXX ships` comment.
- Publish on GitHub release, trusted publishing to PyPI if the org template does that;
  otherwise token secret.

## Acceptance

- Both workflows green on a push to main / a draft release.
