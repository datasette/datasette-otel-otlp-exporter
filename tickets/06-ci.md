# 06 — CI: test + publish workflows

Status: done (pending first push — no GitHub remote exists yet)

The exact CI test command runs green locally with datasette from
`git+https://github.com/simonw/datasette@asg017/otel-phase1-4-otlp-demo`
(branch confirmed pushed to simonw/datasette; local HEAD matches remote tip).
`--no-project` in the workflow matters for the same reason as in the Justfile:
the plugin's own PyPI `datasette` dependency would otherwise shadow the branch
install. Trusted publishing via the `release` environment, matching the
datasette-alerts-ntfy template. Acceptance ("green on a push to main / draft
release") completes once the repo is created and pushed.

Copy `.github/workflows/test.yml` and `publish.yml` from
`~/work/simonw/datasette-alerts-ntfy` and adapt.

## Adaptations

- Test matrix: 3.10–3.13 is enough.
- **Datasette source**: until a datasette alpha ships phase 1, CI must install datasette
  from the git branch:
  `pip install "datasette @ git+https://github.com/simonw/datasette@asg017/otel-phase1-4-otlp-demo"`
  (or the stack tip at the time). Leave a loud `# TODO: unpin when 1.0aXX ships` comment.
- Publish on GitHub release, trusted publishing to PyPI if the org template does that;
  otherwise token secret.

## Acceptance

- Both workflows green on a push to main / a draft release.
